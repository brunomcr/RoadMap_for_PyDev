# Setings


## Poetry
#### New Project
* `poetry new <projectName>`
#### Start a virtuenv
* `poetry env use py`
#### Git (on CD `<patchProject>`)
* `git init`
* `git add <projectName>`
#### Build
* `poetry build`
#### Publish
* `poetry publish`
#### Export Requirements
* `poetry export -o <fileName>.txt`
